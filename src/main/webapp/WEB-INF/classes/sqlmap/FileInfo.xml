<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE sqlMap
    PUBLIC "-//ibatis.apache.org//DTD SQL Map 2.0//EN"
    "http://ibatis.apache.org/dtd/sql-map-2.dtd">

<sqlMap namespace="fileInfo">

	<!-- 別名 -->
	<typeAlias alias="fileInfo" type="jp.co.sji.edu.fb.common.dto.FileInfo" />

	<!-- FileInfo情報を更新する -->
	<insert id="insertFileInfo" parameterClass="fileInfo">
  	<![CDATA[
		INSERT INTO FILEINFO (
			FILEID ,
			FILETYPE ,
  			FILENAME ,
  			ORGFILENAME,
  			FILESIZE ,
  			REGDATE ,
  			LASTUPDATE
		) VALUES(
			#fileID#,
			#fileType#,
			#fileName#,
			#orgFileName#,
			#fileSize#,
			#regDate#,
			sysdate()
		)
    ]]>
	</insert>

	<update id="updateFileInfo" parameterClass="fileInfo">
	 UPDATE FILEINFO
		SET
			FILETYPE = #fileType#,
			FILENAME = #fileName#,
			ORGFILENAME = #orgFileName#,
			FILESIZE = #fileSize#,
			REGDATE = #regDate#,
			LASTUPDATE = sysdate()
		WHERE
			FILEID = #fileID#
	</update>

	<!-- 入力条件より、データを検索する -->
	<select id="selectFileByID" parameterClass="String"
		resultClass="fileInfo">
		SELECT
			FILEID ,
			FILETYPE ,
  			FILENAME ,
  			ORGFILENAME ,
  			FILESIZE ,
  			REGDATE ,
  			LASTUPDATE
		FROM
			FILEINFO
		WHERE
			FILEID = #fileID#
	</select>

	<delete id="deleteFileInfo" parameterClass="String">
		DELETE FROM FILEINFO
		WHERE
			FILEID = #fileID#
	</delete>

</sqlMap>